import React, { useState, useEffect } from 'react';
import { QRCodeCanvas } from 'qrcode.react';
import './ToolsView.css';

// --- SVGs Premium ---
import {
    DollarSign, Clock, Globe, CreditCard, Briefcase, Package,
    Zap, Cpu, Battery, Smartphone, HardDrive, Monitor, Terminal,
    Music, Search, Settings, Wifi, AlertCircle, MessageCircle, QrCode,
    Key, Barcode, X
} from 'lucide-react';

// --- DICCIONARIO DE COLORES (PSICOLOGÍA VISUAL) ---
const COLOR_THEMES = {
    green: { c: '#10b981', bg: 'rgba(16,185,129,0.1)' },
    yellow: { c: '#eab308', bg: 'rgba(234,179,8,0.1)' },
    blue: { c: '#3b82f6', bg: 'rgba(59,130,246,0.1)' },
    purple: { c: '#8b5cf6', bg: 'rgba(139,92,246,0.1)' },
    cyan: { c: '#06b6d4', bg: 'rgba(6,182,212,0.1)' },
    red: { c: '#ef4444', bg: 'rgba(239,68,68,0.1)' },
    orange: { c: '#f97316', bg: 'rgba(249,115,22,0.1)' },
    gray: { c: '#64748b', bg: 'rgba(100,116,139,0.1)' },
    pink: { c: '#ec4899', bg: 'rgba(236,72,153,0.1)' }
};

// COMPONENTE HELPER PARA INPUTS
const InputBox = ({ label, value, onChange, type = "number", placeholder = "", maxl }) => (
    <div className="tool-input-group">
        <label>{label}</label>
        <input type={type} value={value} onChange={e => onChange(e.target.value)} placeholder={placeholder} maxLength={maxl} />
    </div>
);

// COMPONENTE HELPER PARA RESULTADOS
const ResultBox = ({ label, value, colorClass = "" }) => (
    <div className="tool-result-box">
        <div className="res-col" style={{ width: '100%', alignItems: 'center' }}>
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, USD: 'US
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, EUR: '', BRL: 'R
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, CLP: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, MXN: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, COP: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

export default ToolsView;, PEN: 'S/', UYU: '' };
const getCurrencySymbol = (code) => CURRENCY_SYMBOLS[code] || '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;;

            function ToolsView({moneda = 'ARS'}) {
    const sym = getCurrencySymbol(moneda);
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, USD: 'US
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, USD: 'US
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, EUR: '', BRL: 'R
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, CLP: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, MXN: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, COP: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

export default ToolsView;, PEN: 'S/', UYU: '' };
const getCurrencySymbol = (code) => CURRENCY_SYMBOLS[code] || '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;;

            function ToolsView({moneda = 'ARS'}) {
    const sym = getCurrencySymbol(moneda);
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, EUR: '', BRL: 'R
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, USD: 'US
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, EUR: '', BRL: 'R
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, CLP: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, MXN: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, COP: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

export default ToolsView;, PEN: 'S/', UYU: '' };
const getCurrencySymbol = (code) => CURRENCY_SYMBOLS[code] || '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;;

            function ToolsView({moneda = 'ARS'}) {
    const sym = getCurrencySymbol(moneda);
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, CLP: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, USD: 'US
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, EUR: '', BRL: 'R
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, CLP: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, MXN: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, COP: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

export default ToolsView;, PEN: 'S/', UYU: '' };
const getCurrencySymbol = (code) => CURRENCY_SYMBOLS[code] || '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;;

            function ToolsView({moneda = 'ARS'}) {
    const sym = getCurrencySymbol(moneda);
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, MXN: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, USD: 'US
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, EUR: '', BRL: 'R
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, CLP: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, MXN: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, COP: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

export default ToolsView;, PEN: 'S/', UYU: '' };
const getCurrencySymbol = (code) => CURRENCY_SYMBOLS[code] || '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;;

            function ToolsView({moneda = 'ARS'}) {
    const sym = getCurrencySymbol(moneda);
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, COP: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, USD: 'US
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, EUR: '', BRL: 'R
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, CLP: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, MXN: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, COP: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

export default ToolsView;, PEN: 'S/', UYU: '' };
const getCurrencySymbol = (code) => CURRENCY_SYMBOLS[code] || '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;;

            function ToolsView({moneda = 'ARS'}) {
    const sym = getCurrencySymbol(moneda);
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

export default ToolsView;, PEN: 'S/', UYU: '' };\nconst getCurrencySymbol = (code) => CURRENCY_SYMBOLS[code] || '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, USD: 'US
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, EUR: '', BRL: 'R
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, CLP: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, MXN: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, COP: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

export default ToolsView;, PEN: 'S/', UYU: '' };
const getCurrencySymbol = (code) => CURRENCY_SYMBOLS[code] || '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;;

            function ToolsView({moneda = 'ARS'}) {
    const sym = getCurrencySymbol(moneda);
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;;\n\nfunction ToolsView({moneda = 'ARS'}) {\n    const sym = getCurrencySymbol(moneda);\n
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, USD: 'US
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, EUR: '', BRL: 'R
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, CLP: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, MXN: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;, COP: '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

export default ToolsView;, PEN: 'S/', UYU: '' };
const getCurrencySymbol = (code) => CURRENCY_SYMBOLS[code] || '
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;;

            function ToolsView({moneda = 'ARS'}) {
    const sym = getCurrencySymbol(moneda);
            const [busqueda, setBusqueda] = useState('');
            const [activeCategoryModal, setActiveCategoryModal] = useState(null);

            // ==========================================
            // ESTADOS: GESTIÓN Y FINANZAS (8)
            // ==========================================
            const [costo, setCosto] = useState(''); const [venta, setVenta] = useState('');
            const margenNeto = costo && venta ? ((venta - costo) / venta) * 100 : 0;
            const gananciaNeta = costo && venta ? venta - costo : 0;

            const [tarifaHora, setTarifaHora] = useState(''); const [horasTrabajo, setHorasTrabajo] = useState(''); const [minutosTrabajo, setMinutosTrabajo] = useState('');
            const costoManoObra = (parseFloat(tarifaHora) || 0) * ((parseFloat(horasTrabajo) || 0) + ((parseFloat(minutosTrabajo) || 0) / 60));

            const [precioNeto, setPrecioNeto] = useState(''); const [porcentajeIva, setPorcentajeIva] = useState('21');
            const precioBruto = (parseFloat(precioNeto) || 0) * (1 + ((parseFloat(porcentajeIva) || 0) / 100));

            const [precioOriginal, setPrecioOriginal] = useState(''); const [porcentajeDesc, setPorcentajeDesc] = useState('');
            const precioFinalDesc = (parseFloat(precioOriginal) || 0) * (1 - ((parseFloat(porcentajeDesc) || 0) / 100));

            const [usdRate, setUsdRate] = useState(null); const [usdInput, setUsdInput] = useState(''); const [arsInput, setArsInput] = useState('');
    useEffect(() => {fetch('https://api.exchangerate-api.com/v4/latest/USD').then(r => r.json()).then(d => setUsdRate(d.rates.ARS)).catch(); }, []);

    const handleUsdChange = (val) => {setUsdInput(val); setArsInput(val && usdRate ? (parseFloat(val) * usdRate).toFixed(2) : ''); };
    const handleArsChange = (val) => {setArsInput(val); setUsdInput(val && usdRate ? (parseFloat(val) / usdRate).toFixed(2) : ''); };

            const [montoCuota, setMontoCuota] = useState(''); const [interesCuota, setInteresCuota] = useState('15'); const [cantCuotas, setCantCuotas] = useState('3');
            const totalCuotas = montoCuota ? parseFloat(montoCuota) * (1 + (parseFloat(interesCuota) / 100)) : 0;
            const valorPorCuota = cantCuotas ? totalCuotas / parseInt(cantCuotas) : 0;

            const [costoFijoEq, setCostoFijoEq] = useState(''); const [precioVentaEq, setPrecioVentaEq] = useState(''); const [costoVarEq, setCostoVarEq] = useState('');
    const puntoEquilibrio = (precioVentaEq - costoVarEq) > 0 ? Math.ceil(costoFijoEq / (precioVentaEq - costoVarEq)) : 0;

            const [pesoEnvio, setPesoEnvio] = useState(''); const [precioKg, setPrecioKg] = useState(''); const [baseEnvio, setBaseEnvio] = useState('');
            const totalEnvio = (parseFloat(pesoEnvio) || 0) * (parseFloat(precioKg) || 0) + (parseFloat(baseEnvio) || 0);

            // ==========================================
            // ESTADOS: MICROELECTRÓNICA (10)
            // ==========================================
            const [ohmV, setOhmV] = useState(''); const [ohmI, setOhmI] = useState(''); const [ohmR, setOhmR] = useState('');
    const calcOhm = () => {let v = parseFloat(ohmV), i = parseFloat(ohmI), r = parseFloat(ohmR); if (v && i && !r) setOhmR((v / i).toFixed(2)); else if (v && r && !i) setOhmI((v / r).toFixed(2)); else if (i && r && !v) setOhmV((i * r).toFixed(2)); else alert("Llena 2 campos."); };
    const clrOhm = () => {setOhmV(''); setOhmI(''); setOhmR(''); };

            const [wattP, setWattP] = useState(''); const [wattV, setWattV] = useState(''); const [wattI, setWattI] = useState('');
    const calcWatt = () => {let p = parseFloat(wattP), v = parseFloat(wattV), i = parseFloat(wattI); if (v && i && !p) setWattP((v * i).toFixed(2)); else if (p && v && !i) setWattI((p / v).toFixed(2)); else if (p && i && !v) setWattV((p / i).toFixed(2)); else alert("Llena 2 campos."); };
    const clrWatt = () => {setWattP(''); setWattV(''); setWattI(''); };

            const [vIn, setVIn] = useState(''); const [r1, setR1] = useState(''); const [r2, setR2] = useState('');
            const vOut = vIn && r1 && r2 ? (parseFloat(vIn) * (parseFloat(r2) / (parseFloat(r1) + parseFloat(r2)))).toFixed(2) : 0;

            const [b1, setB1] = useState('1'); const [b2, setB2] = useState('0'); const [bMult, setBMult] = useState('100'); const [bTol, setBTol] = useState('5');
    const calcResColor = () => { const v = (parseInt(b1) * 10 + parseInt(b2)) * parseFloat(bMult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [b5_1, setB5_1] = useState('1'); const [b5_2, setB5_2] = useState('0'); const [b5_3, setB5_3] = useState('0'); const [b5_mult, setB5_Mult] = useState('10');
    const calcRes5Color = () => { const v = (parseInt(b5_1) * 100 + parseInt(b5_2) * 10 + parseInt(b5_3)) * parseFloat(b5_mult); return v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v.toFixed(2)} Ω`; };

            const [smdCode, setSmdCode] = useState(''); const [smdResult, setSmdResult] = useState('');
    const calcSmd = () => { if (smdCode.length < 3) return setSmdResult("Inválido"); const b = parseInt(smdCode.substring(0, smdCode.length - 1)), m = Math.pow(10, parseInt(smdCode.slice(-1))), v = b * m; setSmdResult(v >= 1000000 ? `${(v / 1000000).toFixed(2)} MΩ` : v >= 1000 ? `${(v / 1000).toFixed(2)} kΩ` : `${v} Ω`); };

            const [rSerieA, setRSerieA] = useState(''); const [rSerieB, setRSerieB] = useState('');
            const rSerie = (parseFloat(rSerieA) || 0) + (parseFloat(rSerieB) || 0); const rParalelo = rSerieA && rSerieB ? ((rSerieA * rSerieB) / (parseFloat(rSerieA) + parseFloat(rSerieB))).toFixed(2) : 0;

            const [cParA, setCParA] = useState(''); const [cParB, setCParB] = useState('');
            const cParalelo = (parseFloat(cParA) || 0) + (parseFloat(cParB) || 0); const cSerie = cParA && cParB ? ((cParA * cParB) / (parseFloat(cParA) + parseFloat(cParB))).toFixed(2) : 0;

            const [hzInput, setHzInput] = useState('1000');
            const msResult = hzInput ? (1000 / parseFloat(hzInput)).toFixed(4) : 0;

            const [vFuenteLed, setVFuenteLed] = useState(''); const [vCaidaLed, setVCaidaLed] = useState('2.2'); const [iMaLed, setIMaLed] = useState('20');
            const resLed = vFuenteLed && vCaidaLed && iMaLed ? ((vFuenteLed - vCaidaLed) / (iMaLed / 1000)).toFixed(0) : 0;

            // ==========================================
            // ESTADOS: HERRAMIENTAS DE TALLER (7) - INCLUYE NUEVAS
            // ==========================================
            const [awgInput, setAwgInput] = useState('22');
            const awgMap = {"10": {mm: "2.58", a: "30" }, "12": {mm: "2.05", a: "20" }, "14": {mm: "1.62", a: "15" }, "16": {mm: "1.29", a: "10" }, "18": {mm: "1.02", a: "7" }, "20": {mm: "0.81", a: "5" }, "22": {mm: "0.64", a: "3" }, "24": {mm: "0.51", a: "2" }, "26": {mm: "0.40", a: "1" }, "28": {mm: "0.32", a: "0.8" } };

            const [tempC, setTempC] = useState(''); const [tempF, setTempF] = useState('');
    const handleTempC = (val) => {setTempC(val); setTempF(val ? (val * 9 / 5 + 32).toFixed(1) : ''); };
    const handleTempF = (val) => {setTempF(val); setTempC(val ? ((val - 32) * 5 / 9).toFixed(1) : ''); };

            // NUEVAS HERRAMIENTAS DE TALLER:
            const [timer, setTimer] = useState(0); const [timerOn, setTimerOn] = useState(false);
    useEffect(() => {let int; if (timerOn) int = setInterval(() => setTimer(t => t + 1), 1000); return () => clearInterval(int); }, [timerOn]);
    const formatTime = (t) => { const h = Math.floor(t / 3600).toString().padStart(2, '0'), m = Math.floor((t % 3600) / 60).toString().padStart(2, '0'), s = (t % 60).toString().padStart(2, '0'); return `${h}:${m}:${s}`; };

            const [mahAir, setMahAir] = useState(''); const [voltsAir, setVoltsAir] = useState('3.7');
            const whResult = mahAir && voltsAir ? ((parseFloat(mahAir) * parseFloat(voltsAir)) / 1000).toFixed(2) : 0;

            const [capCarga, setCapCarga] = useState(''); const [maCargador, setMaCargador] = useState('');
            const tiempoCarga = capCarga && maCargador ? ((parseFloat(capCarga) / parseFloat(maCargador)) * 1.2).toFixed(1) : 0; // *1.2 para perdida de eficiencia real

            const [wattsConsumo, setWattsConsumo] = useState(''); const [horasDia, setHorasDia] = useState(''); const [precioKwh, setPrecioKwh] = useState('');
            const costoMensualLuz = wattsConsumo && horasDia && precioKwh ? ((parseFloat(wattsConsumo) * parseFloat(horasDia) * 30) / 1000 * parseFloat(precioKwh)).toFixed(2) : 0;

            const [barcodeText, setBarcodeText] = useState('WEPAIRR');

            // ==========================================
            // ESTADOS: DIAGNÓSTICO HARDWARE (8)
            // ==========================================
            const [capDiseno, setCapDiseno] = useState(''); const [capActual, setCapActual] = useState('');
            const saludBateria = capDiseno && capActual ? Math.min(100, Math.max(0, (capActual / capDiseno) * 100)) : 0;

            const [imeiQuery, setImeiQuery] = useState('');
    const checkImei = (e) => {
                e.preventDefault();
            if (!imeiQuery.trim() || imeiQuery.length < 14) return alert("Ingresa un IMEI válido (15 dígitos).");
        navigator.clipboard.writeText(imeiQuery).then(() => {
                alert("✅ IMEI copiado al portapapeles.\n\nSe abrirá IMEI.info. Solo debes darle a 'Pegar'.");
            window.open(`https://www.imei.info/`, '_blank');
        });
    };

            const [almacenamientoGb, setAlmacenamientoGb] = useState('');
            const almacenamientoReal = almacenamientoGb ? (parseFloat(almacenamientoGb) * 0.93132257).toFixed(2) : 0;

            const [resX, setResX] = useState('1920'); const [resY, setResY] = useState('1080'); const [pulgadas, setPulgadas] = useState('6.1');
            const ppiTotal = resX && resY && pulgadas ? (Math.sqrt(Math.pow(resX, 2) + Math.pow(resY, 2)) / pulgadas).toFixed(0) : 0;

            const [tdpCpu, setTdpCpu] = useState('65'); const [tdpGpu, setTdpGpu] = useState('200');
            const psuTotal = (parseFloat(tdpCpu) || 0) + (parseFloat(tdpGpu) || 0) + 100;

            const [deadPixelActive, setDeadPixelActive] = useState(false); const [pixelColorIdx, setPixelColorIdx] = useState(0);
            const pixelColors = ['#000000', '#FFFFFF', '#FF0000', '#00FF00', '#0000FF'];

            const [testKey, setTestKey] = useState('');

    const playTone = (freq) => { const ctx = new (window.AudioContext || window.webkitAudioContext)(); const osc = ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(freq, ctx.currentTime); osc.connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + 1.5); };


            // ==========================================
            // ESTADOS: REDES Y SISTEMAS (5) - INCLUYE NUEVAS
            // ==========================================
            const [datasheetQuery, setDatasheetQuery] = useState('');
    const searchDatasheet = (e) => {e.preventDefault(); window.open(`https://search.alldatasheet.com/v2/exec/search.jsp?sSearchword=${encodeURIComponent(datasheetQuery)}`, '_blank'); };

            const [firmwareBrand, setFirmwareBrand] = useState('apple'); const [firmwareModel, setFirmwareModel] = useState('');
    const searchFirmware = (e) => {e.preventDefault(); if (firmwareBrand === 'apple') window.open(`https://ipsw.me/`, '_blank'); else if (firmwareBrand === 'samsung') window.open(`https://samfw.com/search/${encodeURIComponent(firmwareModel)}`, '_blank'); else window.open(`https://mifirm.net/model/${encodeURIComponent(firmwareModel.toLowerCase())}`, '_blank'); };

            const [macQuery, setMacQuery] = useState('');
    const checkMac = (e) => {e.preventDefault(); if (macQuery) window.open(`https://macvendors.com/query/${macQuery}`, '_blank'); };

            // NUEVAS REDES Y SISTEMAS
            const [myIp, setMyIp] = useState('');
    const getIp = async () => { try { const r = await fetch('https://api.ipify.org?format=json'); const d = await r.json(); setMyIp(d.ip); } catch (e) {setMyIp('Error'); } };

            const [bsodCode, setBsodCode] = useState('');
    const searchBsod = (e) => {e.preventDefault(); if (bsodCode) window.open(`https://www.google.com/search?q=Windows+BSOD+${encodeURIComponent(bsodCode)}`, '_blank'); };

            const [dataConvInput, setDataConvInput] = useState(''); const [dataConvUnit, setDataConvUnit] = useState('GB');
    const calcDataConv = () => { const v = parseFloat(dataConvInput) || 0; if (dataConvUnit === 'GB') return `${(v * 1024).toFixed(0)} MB`; if (dataConvUnit === 'MB') return `${(v / 1024).toFixed(2)} GB`; if (dataConvUnit === 'TB') return `${(v * 1024).toFixed(0)} GB`; return '0'; };

            // ==========================================
            // ESTADOS: COMUNICACIÓN Y MARKETING (8) - INCLUYE NUEVAS
            // ==========================================
            const [waNumero, setWaNumero] = useState(''); const [waMensaje, setWaMensaje] = useState('');
    const enviarWa = () => { if (!waNumero) return alert("Ingresa un número"); window.open(`https://wa.me/${waNumero.replace(/\D/g, '')}?text=${encodeURIComponent(waMensaje)}`, '_blank'); };

            const [qrText, setQrText] = useState('https://wepairr.com');

            const [warrantyType, setWarrantyType] = useState('pantalla');
            const warrantyTexts = {'pantalla': "La garantía de la pantalla cubre exclusivamente fallas de táctil espontáneas o defectos de imagen no originados por golpes. NO CUBRE: roturas, rayones o presión.", 'bateria': "La garantía de la batería cubre descargas irregulares. NO CUBRE: desgaste natural (ciclos) o baterías infladas por cargadores genéricos.", 'placa': "La reparación en placa base cuenta con garantía sobre el circuito específico reparado. NO CUBRE: fallas ajenas a la reparación, humedad o golpes." };
    const copyWarranty = () => {navigator.clipboard.writeText(warrantyTexts[warrantyType]); alert("Copiado."); };

            const [genPassword, setGenPassword] = useState('');
    const generatePass = () => { const c = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*"; let p = ""; for (let i = 0; i < 16; i++) p += c.charAt(Math.floor(Math.random() * c.length)); setGenPassword(p); };

            const [formatText, setFormatText] = useState('');
    const copyFormat = (type) => { if (!formatText) return; let f = formatText; if (type === 'bold') f = `*${f}*`; if (type === 'italic') f = `_${f}_`; if (type === 'strike') f = `~${f}~`; navigator.clipboard.writeText(f); alert("Copiado con formato WA."); };

            // NUEVAS COMUNICACIÓN Y MARKETING
            const [promoType, setPromoType] = useState('descuento');
    const getPromoText = () => { if (promoType === 'descuento') return "¡Aprovecha! 10% de descuento en cambios de pantalla. 📱✨ Envíanos un mensaje."; if (promoType === 'bateria') return "¿Tu celular se apaga rápido? 🔋 Cambiamos tu batería en el día. ¡Consultanos!"; return "Tu PC lenta tiene solución 💻 Limpieza térmica y SSD. Presupuestos sin cargo."; };


            // ==========================================
            // ESTRUCTURA MAESTRA DE 46 HERRAMIENTAS
            // ==========================================
            const TOOLS_DATA = [
            // --- 1. FINANZAS Y NEGOCIO ---
            {
                cat: 'Finanzas y Negocio', id: '1', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Margen y Ganancia', desc: 'Rentabilidad neta repuestos.', keys: 'dinero costo venta', render: () => (
            <><InputBox label="Costo ()" value={costo} onChange={setCosto} /><InputBox label="Venta ()" value={venta} onChange={setVenta} /><ResultBox label="Margen" value={`${margenNeto.toFixed(1)}%`} colorClass={margenNeto < 30 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '2', colorTheme: 'yellow', icon: <Clock size={24} strokeWidth={2} />, title: 'Mano de Obra', desc: 'Cobro por tiempo invertido.', keys: 'tiempo hora reloj', render: () => (
            <><InputBox label="Tarifa/Hora ()" value={tarifaHora} onChange={setTarifaHora} /><div className="grid-2-col"><InputBox label="Horas" value={horasTrabajo} onChange={setHorasTrabajo} /><InputBox label="Minutos" value={minutosTrabajo} onChange={setMinutosTrabajo} /></div><ResultBox label="Total" value={`$${costoManoObra.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '3', colorTheme: 'green', icon: <DollarSign size={24} strokeWidth={2} />, title: 'IVA / Impuestos', desc: 'Suma % a tu tarifa.', keys: 'iva afip taxes', render: () => (
            <><InputBox label="Neto ()" value={precioNeto} onChange={setPrecioNeto} /><InputBox label="IVA (%)" value={porcentajeIva} onChange={setPorcentajeIva} /><ResultBox label="Bruto Final" value={`$${precioBruto.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '4', colorTheme: 'yellow', icon: <DollarSign size={24} strokeWidth={2} />, title: 'Descuentos', desc: 'Aplica rebajas al cliente.', keys: 'promo descuento rebaja', render: () => (
            <><InputBox label="Original ()" value={precioOriginal} onChange={setPrecioOriginal} /><InputBox label="Desc. (%)" value={porcentajeDesc} onChange={setPorcentajeDesc} /><ResultBox label="Precio Final" value={`$${precioFinalDesc.toFixed(2)}`} colorClass="text-success" /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '5', colorTheme: 'green', icon: <Globe size={24} strokeWidth={2} />, title: 'Divisas (En Vivo)', desc: 'Cotización USD global.', keys: 'dolar euro pesos', render: () => (
            <><p className="tool-hint">1 USD = ${usdRate || '...'} ARS</p><div className="grid-2-col"><InputBox label="USD" value={usdInput} onChange={handleUsdChange} /><InputBox label="ARS" value={arsInput} onChange={handleArsChange} /></div></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '6', colorTheme: 'yellow', icon: <CreditCard size={24} strokeWidth={2} />, title: 'Cuotas / Interés', desc: 'Interés por financiamiento.', keys: 'posnet mercadopago', render: () => (
            <><InputBox label="Monto ()" value={montoCuota} onChange={setMontoCuota} /><div className="grid-2-col"><InputBox label="Interés (%)" value={interesCuota} onChange={setInteresCuota} /><InputBox label="Cuotas" value={cantCuotas} onChange={setCantCuotas} /></div><ResultBox label="Cada Cuota" value={`$${valorPorCuota.toFixed(2)}`} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '7', colorTheme: 'gray', icon: <Briefcase size={24} strokeWidth={2} />, title: 'Punto Equilibrio', desc: 'Unidades para no perder.', keys: 'balance costos', render: () => (
            <><InputBox label="Costos Fijos ()" value={costoFijoEq} onChange={setCostoFijoEq} /><div className="grid-2-col"><InputBox label="Precio Venta Unit." value={precioVentaEq} onChange={setPrecioVentaEq} /><InputBox label="Costo Var. Unit." value={costoVarEq} onChange={setCostoVarEq} /></div><ResultBox label="Unidades necesarias" value={puntoEquilibrio} /></>
            )
        },
            {
                cat: 'Finanzas y Negocio', id: '8', colorTheme: 'gray', icon: <Package size={24} strokeWidth={2} />, title: 'Costo de Envío', desc: 'Cálculo de fletes.', keys: 'paquete correo logistica', render: () => (
            <><InputBox label="Peso (Kg)" value={pesoEnvio} onChange={setPesoEnvio} /><div className="grid-2-col"><InputBox label="Precio x Kg" value={precioKg} onChange={setPrecioKg} /><InputBox label="Base ()" value={baseEnvio} onChange={setBaseEnvio} /></div><ResultBox label="Costo Envío" value={`$${totalEnvio.toFixed(2)}`} /></>
            )
        },

            // --- 2. MICROELECTRÓNICA ---
            {
                cat: 'Microelectrónica', id: '9', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Ohm', desc: 'V = I x R', keys: 'voltaje corriente ohmio', render: () => (
            <><div className="grid-2-col"><InputBox label="V" value={ohmV} onChange={setOhmV} /><InputBox label="I" value={ohmI} onChange={setOhmI} /></div><InputBox label="R" value={ohmR} onChange={setOhmR} /><div className="tool-action-row"><button onClick={calcOhm} className="btn-tool-action primary">Calcular</button><button onClick={clrOhm} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '10', colorTheme: 'purple', icon: <Zap size={24} strokeWidth={2} />, title: 'Ley de Watt', desc: 'P = V x I', keys: 'potencia watts', render: () => (
            <><div className="grid-2-col"><InputBox label="W" value={wattP} onChange={setWattP} /><InputBox label="V" value={wattV} onChange={setWattV} /></div><InputBox label="I" value={wattI} onChange={setWattI} /><div className="tool-action-row"><button onClick={calcWatt} className="btn-tool-action primary">Calcular</button><button onClick={clrWatt} className="btn-tool-action secondary">Limpiar</button></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '11', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Divisor de Tensión', desc: 'Caída Vout en 2 R.', keys: 'divisor caida voltaje', render: () => (
            <><InputBox label="Vin" value={vIn} onChange={setVIn} /><div className="grid-2-col"><InputBox label="R1" value={r1} onChange={setR1} /><InputBox label="R2" value={r2} onChange={setR2} /></div><ResultBox label="V Out" value={`${vOut} V`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '12', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 4 Bandas', desc: 'R DIP clásica THT.', keys: 'resistencias colores', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b1} onChange={e => setB1(e.target.value)} className="resistor-select"><option value="1">Marrón</option><option value="2">Rojo</option></select></div><div className="tool-input-group"><label>B2</label><select value={b2} onChange={e => setB2(e.target.value)} className="resistor-select"><option value="0">Negro</option><option value="1">Marrón</option></select></div><div className="tool-input-group"><label>Mult</label><select value={bMult} onChange={e => setBMult(e.target.value)} className="resistor-select"><option value="100">x100</option></select></div><div className="tool-input-group"><label>Tol</label><select value={bTol} onChange={e => setBTol(e.target.value)} className="resistor-select"><option value="5">5%</option></select></div></div><ResultBox label="Valor" value={calcResColor()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '13', colorTheme: 'blue', icon: <Cpu size={24} strokeWidth={2} />, title: 'Resistor 5 Bandas', desc: 'R de alta precisión.', keys: 'precision ohms 5', render: () => (
            <><div className="grid-2-col"><div className="tool-input-group"><label>B1</label><select value={b5_1} onChange={e => setB5_1(e.target.value)} className="resistor-select"><option value="1">Marr</option></select></div><div className="tool-input-group"><label>B2</label><select value={b5_2} onChange={e => setB5_2(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div></div><div className="grid-2-col"><div className="tool-input-group"><label>B3</label><select value={b5_3} onChange={e => setB5_3(e.target.value)} className="resistor-select"><option value="0">Negro</option></select></div><div className="tool-input-group"><label>Mult</label><select value={b5_mult} onChange={e => setB5_Mult(e.target.value)} className="resistor-select"><option value="10">x10</option></select></div></div><ResultBox label="Valor Precisión" value={calcRes5Color()} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '14', colorTheme: 'purple', icon: <Cpu size={24} strokeWidth={2} />, title: 'Códigos SMD', desc: 'Traductor impreso.', keys: 'smd placa smt', render: () => (
            <><InputBox label="Código (Ej: 103)" value={smdCode} onChange={setSmdCode} type="text" maxl="4" /><button onClick={calcSmd} className="btn-tool-action primary">Traducir</button><ResultBox label="Valor Real" value={smdResult || '-'} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '15', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Res Serie/Paralelo', desc: 'Suma de equivalentes.', keys: 'equivalente suma r', render: () => (
            <><div className="grid-2-col"><InputBox label="R1" value={rSerieA} onChange={setRSerieA} /><InputBox label="R2" value={rSerieB} onChange={setRSerieB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie:</span><strong>{rSerie} Ω</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo:</span><strong>{rParalelo} Ω</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '16', colorTheme: 'gray', icon: <Cpu size={24} strokeWidth={2} />, title: 'Cap Serie/Paralelo', desc: 'Inverso a Resistencias.', keys: 'capacitor faradios suma', render: () => (
            <><div className="grid-2-col"><InputBox label="C1" value={cParA} onChange={setCParA} /><InputBox label="C2" value={cParB} onChange={setCParB} /></div><div className="tool-result-box" style={{ flexDirection: 'column', gap: '10px' }}><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Paralelo (+):</span><strong>{cParalelo} F</strong></div><div style={{ display: 'flex', justifyContent: 'space-between' }}><span>Serie (Inv):</span><strong>{cSerie} F</strong></div></div></>
            )
        },
            {
                cat: 'Microelectrónica', id: '18', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Oscilador Frec.', desc: 'Hz a milisegundos.', keys: 'osciloscopio onda tiempo', render: () => (
            <><InputBox label="Frecuencia (Hz)" value={hzInput} onChange={setHzInput} /><ResultBox label="Periodo (T)" value={`${msResult} ms`} /></>
            )
        },
            {
                cat: 'Microelectrónica', id: '19', colorTheme: 'yellow', icon: <Zap size={24} strokeWidth={2} />, title: 'Resistencia para LED', desc: 'Protección de diodos.', keys: 'led iluminacion diodo', render: () => (
            <><div className="grid-2-col"><InputBox label="V Fuente" value={vFuenteLed} onChange={setVFuenteLed} /><InputBox label="V Caída LED" value={vCaidaLed} onChange={setVCaidaLed} /></div><InputBox label="Corriente (mA)" value={iMaLed} onChange={setIMaLed} /><ResultBox label="Res Sugerida" value={`${resLed} Ω`} /></>
            )
        },

            // --- 3. DIAGNÓSTICO HARDWARE ---
            {
                cat: 'Diagnóstico Hardware', id: '20', colorTheme: 'cyan', icon: <Battery size={24} strokeWidth={2} />, title: 'Salud Batería', desc: 'Desgaste químico real.', keys: 'mah vida iphone', render: () => (
            <><InputBox label="Fábrica (mAh)" value={capDiseno} onChange={setCapDiseno} /><InputBox label="Actual (mAh)" value={capActual} onChange={setCapActual} /><ResultBox label="Vida Útil" value={`${saludBateria.toFixed(1)}%`} colorClass={saludBateria < 80 ? 'text-danger' : 'text-success'} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '21', colorTheme: 'red', icon: <Smartphone size={24} strokeWidth={2} />, title: 'IMEI Check Global', desc: 'Blacklist mundial.', keys: 'reporte banda negativa', render: () => (
            <><form onSubmit={checkImei} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={imeiQuery} onChange={e => setImeiQuery(e.target.value)} placeholder="15 dígitos..." maxLength="15" required /></div><button type="submit" className="btn-tool-action primary">Copiar y Chequear</button></form></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '22', colorTheme: 'gray', icon: <HardDrive size={24} strokeWidth={2} />, title: 'GB a GiB (Discos)', desc: 'Espacio formateado.', keys: 'ssd hdd almacenamiento', render: () => (
            <><InputBox label="Caja (GB)" value={almacenamientoGb} onChange={setAlmacenamientoGb} /><ResultBox label="Windows (GiB)" value={`${almacenamientoReal} GiB`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '23', colorTheme: 'cyan', icon: <Monitor size={24} strokeWidth={2} />, title: 'Densidad PPI', desc: 'Píxeles por pulgada.', keys: 'pantalla resolucion display', render: () => (
            <><div className="grid-2-col"><InputBox label="X" value={resX} onChange={setResX} /><InputBox label="Y" value={resY} onChange={setResY} /></div><InputBox label="Pulgadas" value={pulgadas} onChange={setPulgadas} /><ResultBox label="Densidad" value={`${ppiTotal} PPI`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '24', colorTheme: 'red', icon: <Zap size={24} strokeWidth={2} />, title: 'PSU Calc', desc: 'Fuente de PC Gamer.', keys: 'watts consumo energia', render: () => (
            <><div className="grid-2-col"><InputBox label="TDP CPU" value={tdpCpu} onChange={setTdpCpu} /><InputBox label="TDP GPU" value={tdpGpu} onChange={setTdpGpu} /></div><ResultBox label="Fuente Mínima" value={`${psuTotal} W`} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '25', colorTheme: 'orange', icon: <Monitor size={24} strokeWidth={2} />, title: 'Test Píxeles Muertos', desc: 'Full screen RGB.', keys: 'mancha display lcd', render: () => (
            <><p className="tool-hint">Busca fugas de luz o píxeles trabados.</p><button onClick={() => setDeadPixelActive(true)} className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Iniciar Test RGB</button></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '26', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'Test Teclado', desc: 'Códigos Keycode.', keys: 'notebook key ghost', render: () => (
            <><p className="tool-hint">Presiona teclas dudosas:</p><input type="text" onKeyDown={e => { e.preventDefault(); setTestKey(`[${e.key}] - Code: ${e.keyCode}`); }} value={testKey} placeholder="Toca una tecla..." readOnly className="tool-input-group" style={{ padding: '15px', textAlign: 'center', fontWeight: 'bold', marginTop: 'auto' }} /></>
            )
        },
            {
                cat: 'Diagnóstico Hardware', id: '27', colorTheme: 'purple', icon: <Music size={24} strokeWidth={2} />, title: 'Test Audio (Osc)', desc: 'Generador tonos puros.', keys: 'parlante speaker sonido', render: () => (
            <><p className="tool-hint">Prueba parlantes saturados.</p><div className="grid-2-col" style={{ marginTop: 'auto' }}><button onClick={() => playTone(200)} className="btn-tool-action secondary">200 Hz</button><button onClick={() => playTone(1000)} className="btn-tool-action secondary">1 kHz</button></div></>
            )
        },

            // --- 4. HERRAMIENTAS DE TALLER ---
            {
                cat: 'Herramientas de Taller', id: '33', colorTheme: 'pink', icon: <Clock size={24} strokeWidth={2} />, title: 'Cronómetro Taller', desc: 'Mide tiempo de trabajo.', keys: 'hora reloj timer', render: () => (
            <><ResultBox label="Transcurrido" value={formatTime(timer)} colorClass="text-primary" /><div className="grid-2-col" style={{ marginTop: '10px' }}><button onClick={() => setTimerOn(!timerOn)} className={`btn-tool-action ${timerOn ? 'secondary' : 'primary'}`}>{timerOn ? 'Pausar' : 'Iniciar'}</button><button onClick={() => { setTimerOn(false); setTimer(0); }} className="btn-tool-action secondary">Reset</button></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '17_b', colorTheme: 'gray', icon: <Settings size={24} strokeWidth={2} />, title: 'AWG a Amperes', desc: 'Corriente Max Cable.', keys: 'hilo cobre calibre awg', render: () => (
            <><div className="tool-input-group"><label>Calibre AWG</label><select value={awgInput} onChange={e => setAwgInput(e.target.value)} className="resistor-select">{Object.keys(awgMap).map(k => <option key={k} value={k}>{k} AWG</option>)}</select></div><div className="grid-2-col"><ResultBox label="Grosor" value={`${awgMap[awgInput]?.mm}mm`} /><ResultBox label="Soporta" value={`${awgMap[awgInput]?.a}A`} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '28', colorTheme: 'red', icon: <Monitor size={24} strokeWidth={2} />, title: 'Conv. Temperaturas', desc: 'Cautín °C a °F.', keys: 'soldador calor temp', render: () => (
            <><div className="grid-2-col"><InputBox label="°C" value={tempC} onChange={handleTempC} /><InputBox label="°F" value={tempF} onChange={handleTempF} /></div></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '36', colorTheme: 'yellow', icon: <Battery size={24} strokeWidth={2} />, title: 'mAh a Wh (Vuelos)', desc: 'Límites batería TSA.', keys: 'avion viaje capacidad', render: () => (
            <><div className="grid-2-col"><InputBox label="mAh" value={mahAir} onChange={setMahAir} /><InputBox label="Volts" value={voltsAir} onChange={setVoltsAir} /></div><ResultBox label="Energía" value={`${whResult} Wh`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '37', colorTheme: 'cyan', icon: <Clock size={24} strokeWidth={2} />, title: 'Tiempo de Carga', desc: 'Horas hasta 100%.', keys: 'bateria cargador lento', render: () => (
            <><div className="grid-2-col"><InputBox label="Bat (mAh)" value={capCarga} onChange={setCapCarga} /><InputBox label="Cargador (mA)" value={maCargador} onChange={setMaCargador} /></div><ResultBox label="Tiempo Est." value={`${tiempoCarga} Hrs`} /></>
            )
        },
            {
                cat: 'Herramientas de Taller', id: '39', colorTheme: 'orange', icon: <Zap size={24} strokeWidth={2} />, title: 'Consumo Eléctrico', desc: 'Gasto luz equipos.', keys: 'kw kwh dinero facturacion', render: () => (
            <><div className="grid-2-col"><InputBox label="Watts" value={wattsConsumo} onChange={setWattsConsumo} /><InputBox label="Horas/Día" value={horasDia} onChange={setHorasDia} /></div><InputBox label="Precio KWh ()" value={precioKwh} onChange={setPrecioKwh} /><ResultBox label="Costo Mensual" value={`$${costoMensualLuz}`} /></>
            )
        },

            // --- 5. REDES Y SISTEMAS ---
            {
                cat: 'Redes y Sistemas', id: '28_b', colorTheme: 'blue', icon: <Settings size={24} strokeWidth={2} />, title: 'Datasheets', desc: 'Buscador oficial PDFs.', keys: 'integrado chip boardview', render: () => (
            <><form onSubmit={searchDatasheet} style={{ display: 'flex', flexDirection: 'column', gap: '15px', marginTop: 'auto' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={datasheetQuery} onChange={e => setDatasheetQuery(e.target.value)} required /></div><button type="submit" className="btn-tool-action primary">Buscar PDF</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '29_b', colorTheme: 'orange', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Firmwares / ROMs', desc: 'Sistemas de fábrica.', keys: 'flasheo odin ipsw', render: () => (
            <><form onSubmit={searchFirmware} style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}><div className="tool-input-group"><select value={firmwareBrand} onChange={e => setFirmwareBrand(e.target.value)} className="resistor-select"><option value="apple">Apple</option><option value="samsung">Samsung</option><option value="xiaomi">Xiaomi</option></select></div>{firmwareBrand !== 'apple' && <InputBox label="Modelo" value={firmwareModel} onChange={setFirmwareModel} type="text" />}<button type="submit" className="btn-tool-action primary" style={{ marginTop: 'auto' }}>Buscar ROM</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '30_b', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Verificador IP', desc: 'IP Pública actual.', keys: 'red internet router', render: () => (
            <><ResultBox label="Tu IP Pública" value={myIp || '0.0.0.0'} /><button onClick={getIp} className="btn-tool-action secondary" style={{ marginTop: '10px' }}>Obtener IP</button></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '31_b', colorTheme: 'gray', icon: <Terminal size={24} strokeWidth={2} />, title: 'MAC OUI Lookup', desc: 'Fabricante placa red.', keys: 'ip mac address router', render: () => (
            <><form onSubmit={checkMac} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={macQuery} onChange={e => setMacQuery(e.target.value)} placeholder="00:1A:2B..." required /></div><button type="submit" className="btn-tool-action primary">Identificar Marca</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '32_b', colorTheme: 'red', icon: <AlertCircle size={24} strokeWidth={2} />, title: 'Buscador BSOD', desc: 'Pantallazos Azules.', keys: 'windows error crash', render: () => (
            <><form onSubmit={searchBsod} style={{ display: 'flex', flexDirection: 'column', gap: '15px' }}><div className="tool-search-input"><Search size={20} strokeWidth={2} /><input type="text" value={bsodCode} onChange={e => setBsodCode(e.target.value)} placeholder="0x0000007B..." required /></div><button type="submit" className="btn-tool-action primary">Buscar en Google</button></form></>
            )
        },
            {
                cat: 'Redes y Sistemas', id: '38_b', colorTheme: 'purple', icon: <HardDrive size={24} strokeWidth={2} />, title: 'Conversor Datos', desc: 'GB a MB/TB.', keys: 'peso archivo gigas', render: () => (
            <><div className="grid-2-col"><InputBox label="Cantidad" value={dataConvInput} onChange={setDataConvInput} /><div className="tool-input-group"><label>Unidad</label><select value={dataConvUnit} onChange={e => setDataConvUnit(e.target.value)} className="resistor-select"><option value="MB">MB</option><option value="GB">GB</option><option value="TB">TB</option></select></div></div><ResultBox label="Equivalencia" value={calcDataConv()} /></>
            )
        },

            // --- 6. COMUNICACIÓN Y MARKETING ---
            {
                cat: 'Comunicación y Marketing', id: '40', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'WA Directo', desc: 'Chat sin agendar.', keys: 'whatsapp mensaje', render: () => (
            <><InputBox label="Número" value={waNumero} onChange={setWaNumero} type="text" /><div className="tool-input-group"><textarea rows="2" value={waMensaje} onChange={e => setWaMensaje(e.target.value)} placeholder="Mensaje..."></textarea></div><button onClick={enviarWa} className="btn-tool-action" style={{ background: '#25D366', color: 'white' }}>Abrir Chat</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '41', colorTheme: 'cyan', icon: <Wifi size={24} strokeWidth={2} />, title: 'Generador QR', desc: 'Links para clientes.', keys: 'link escanear wifi', render: () => (
            <><InputBox label="Texto o URL" value={qrText} onChange={setQrText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', padding: '10px', background: 'white', borderRadius: '12px' }}><QRCodeCanvas value={qrText || ' '} size={100} /></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '42', colorTheme: 'gray', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Garantías Legales', desc: 'Plantillas de texto.', keys: 'terminos cliente ticket', render: () => (
            <><div className="tool-input-group"><select value={warrantyType} onChange={e => setWarrantyType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="pantalla">Pantallas</option><option value="bateria">Baterías</option><option value="placa">Placas Base</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-secondary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px', overflowY: 'auto' }}>{warrantyTexts[warrantyType]}</div><button onClick={copyWarranty} className="btn-tool-action secondary">Copiar Texto Legal</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '43', colorTheme: 'red', icon: <Key size={24} strokeWidth={2} />, title: 'Generador Claves', desc: 'Seguridad Routers.', keys: 'password wifi wpa', render: () => (
            <><div className="tool-search-input" style={{ marginBottom: '10px', justifyContent: 'center', fontWeight: 'bold', letterSpacing: '1px' }}>{genPassword || '...'}</div><button onClick={generatePass} className="btn-tool-action primary">Generar Segura</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '44', colorTheme: 'green', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Formato WhatsApp', desc: 'Copia texto enriquecido.', keys: 'texto cursiva negrita', render: () => (
            <><InputBox label="Texto" value={formatText} onChange={setFormatText} type="text" /><div className="grid-2-col"><button onClick={() => copyFormat('bold')} className="btn-tool-action secondary">Negrita</button><button onClick={() => copyFormat('strike')} className="btn-tool-action secondary">Tachado</button></div></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '45', colorTheme: 'pink', icon: <MessageCircle size={24} strokeWidth={2} />, title: 'Promos Sociales', desc: 'Textos de Marketing.', keys: 'instagram facebook promocion', render: () => (
            <><div className="tool-input-group"><select value={promoType} onChange={e => setPromoType(e.target.value)} className="resistor-select" style={{ marginBottom: '10px' }}><option value="descuento">Promo Pantallas</option><option value="bateria">Promo Baterías</option><option value="pc">Promo PC</option></select></div><div style={{ fontSize: '0.85rem', color: 'var(--text-primary)', background: 'var(--bg-input-glass)', padding: '10px', borderRadius: '8px', marginBottom: '10px', height: '80px' }}>{getPromoText()}</div><button onClick={() => { navigator.clipboard.writeText(getPromoText()); alert("Copiado!") }} className="btn-tool-action primary">Copiar para Redes</button></>
            )
        },
            {
                cat: 'Comunicación y Marketing', id: '46', colorTheme: 'gray', icon: <Barcode size={24} strokeWidth={2} />, title: 'Gen. Código Barras', desc: 'Para etiquetas.', keys: 'barcode impresora etiqueta', render: () => (
            <><InputBox label="ID Equipo / Ticket" value={barcodeText} onChange={setBarcodeText} type="text" /><div style={{ display: 'flex', justifyContent: 'center', marginTop: '10px', background: 'white', padding: '10px', borderRadius: '8px' }}><img src={`https://barcode.tec-it.com/barcode.ashx?data=${encodeURIComponent(barcodeText || 'WEPAIRR')}&code=Code128&dpi=96`} alt="Barcode" style={{ maxWidth: '100%' }} /></div></>
            )
        }
            ];

    // ==========================================
    // RENDERIZADO VISUAL
    // ==========================================
    const categoryNames = [...new Set(TOOLS_DATA.map(t => t.cat))];

    const herramientasFiltradas = TOOLS_DATA.filter(tool => {
        if (busqueda.trim() !== '') {
            const query = busqueda.toLowerCase();
            return tool.title.toLowerCase().includes(query) || tool.keys.includes(query) || tool.cat.toLowerCase().includes(query);
        }
            return activeCategoryModal === 'Todas' || !activeCategoryModal || tool.cat === activeCategoryModal;
    });

            const isSearching = busqueda.trim() !== '';

            return (
            <div className="tools-layout-modern animate-fade-in">

                {/* PIXEL TEST EN PANTALLA COMPLETA */}
                {deadPixelActive && (
                    <div onClick={() => setPixelColorIdx((pixelColorIdx + 1) % pixelColors.length)} style={{ position: 'fixed', top: 0, left: 0, width: '100vw', height: '100vh', zIndex: 999999, background: pixelColors[pixelColorIdx], cursor: 'crosshair' }}>
                        <div style={{ background: 'rgba(0,0,0,0.7)', color: 'white', padding: '10px 20px', position: 'absolute', top: 20, left: 20, borderRadius: '8px', cursor: 'pointer', fontWeight: 'bold', backdropFilter: 'blur(5px)' }} onClick={(e) => { e.stopPropagation(); setDeadPixelActive(false) }}>
                            Salir del Test (Click aquí)
                        </div>
                    </div>
                )}

                {/* VISTA 1: DASHBOARD DE CATEGORIAS (BLOQUES) */}
                {!isSearching && !activeCategoryModal && (
                    <div className="categories-dashboard animate-fade-in">
                        <header className="tools-header-main">
                            <h2 style={{ fontSize: '2.5rem', margin: '0 0 10px 0', color: 'var(--text-primary)', fontWeight: '900', letterSpacing: '-1px' }}>Caja de Herramientas</h2>
                            <p style={{ color: 'var(--text-secondary)', margin: 0, fontSize: '1.1rem' }}>46 Utilidades avanzadas para el taller moderno.</p>

                            <div className="tools-search-bar glass-effect" style={{ maxWidth: '600px', marginTop: '20px' }}>
                                <Search size={20} strokeWidth={2} />
                                <input type="text" placeholder="Buscar herramienta (Ej: ohm, bateria, iva, firmware)..." value={busqueda} onChange={e => setBusqueda(e.target.value)} />
                            </div>
                        </header>

                        <div className="categories-grid">
                            {categoryNames.map((cat, idx) => {
                                const count = TOOLS_DATA.filter(t => t.cat === cat).length;
                                // Asignamos un color base predefinido a cada bloque para que se vean increíbles
                                const blockColors = ['green', 'purple', 'cyan', 'gray', 'red', 'pink'];
                                const theme = COLOR_THEMES[blockColors[idx % blockColors.length]];

                                return (
                                    <div key={cat} className="category-block glass-effect" style={{ '--hover-border': theme.c }} onClick={() => setActiveCategoryModal(cat)}>
                                        <div className="cat-block-header">
                                            <div className="cat-icon-large" style={{ color: theme.c, background: theme.bg }}>
                                                <Settings size={24} strokeWidth={2} />
                                            </div>
                                            <span className="cat-count-badge">{count} Herramientas</span>
                                        </div>
                                        <h3>{cat}</h3>
                                        <p>Toca para abrir la suite completa.</p>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                )}

                {/* VISTA 2: RESULTADOS DE BÚSQUEDA */}
                {isSearching && (
                    <div className="search-results-dashboard animate-fade-in">
                        <div className="tools-search-bar glass-effect" style={{ marginBottom: '30px' }}>
                            <Search size={20} strokeWidth={2} />
                            <input type="text" placeholder="Buscando..." value={busqueda} onChange={e => setBusqueda(e.target.value)} autoFocus />
                            <button onClick={() => setBusqueda('')} style={{ background: 'transparent', border: 'none', color: 'var(--text-secondary)', cursor: 'pointer' }}><X size={24} strokeWidth={2} /></button>
                        </div>

                        <div className="tools-grid">
                            {herramientasFiltradas.length === 0 ? (
                                <div className="tools-empty-state">No se encontraron herramientas. Intenta con otros términos.</div>
                            ) : (
                                herramientasFiltradas.map(tool => (
                                    <div key={tool.id} className="tool-card glass-effect" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                        <div className="tool-header">
                                            <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                {tool.icon}
                                            </div>
                                            <div className="tool-header-text">
                                                <h3>{tool.title}</h3>
                                                <span className="tool-cat-badge">{tool.cat}</span>
                                            </div>
                                        </div>
                                        <div className="tool-body">{tool.render()}</div>
                                    </div>
                                ))
                            )}
                        </div>
                    </div>
                )}

                {/* VISTA 3: MODAL INMERSIVO DE SUITE ELEGIDA */}
                {activeCategoryModal && !isSearching && (
                    <div className="tools-modal-overlay animate-fade-in" onClick={() => setActiveCategoryModal(null)}>
                        <div className="tools-modal-container glass-effect animate-scale-in" onClick={e => e.stopPropagation()}>

                            <div className="tools-modal-header">
                                <div style={{ display: 'flex', flexDirection: 'column' }}>
                                    <span className="tools-modal-sup">Suite de Utilidades</span>
                                    <h2>{activeCategoryModal}</h2>
                                </div>
                                <button className="btn-close-tools" onClick={() => setActiveCategoryModal(null)}><X size={24} strokeWidth={2} /></button>
                            </div>

                            <div className="tools-modal-body">
                                <div className="tools-grid">
                                    {TOOLS_DATA.filter(t => t.cat === activeCategoryModal).map(tool => (
                                        <div key={tool.id} className="tool-card inside-modal" style={{ borderColor: COLOR_THEMES[tool.colorTheme]?.c }}>
                                            <div className="tool-header">
                                                <div className="tool-icon" style={{ color: COLOR_THEMES[tool.colorTheme]?.c, background: COLOR_THEMES[tool.colorTheme]?.bg }}>
                                                    {tool.icon}
                                                </div>
                                                <div className="tool-header-text">
                                                    <h3>{tool.title}</h3>
                                                    <p className="tool-hint" style={{ margin: 0 }}>{tool.desc}</p>
                                                </div>
                                            </div>
                                            <div className="tool-body">{tool.render()}</div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                )}

            </div>
            );
}

            export default ToolsView;